<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>nanoFramework.Migrate.Tests</RootNamespace>
    <IsPackable>false</IsPackable>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\NanoMigrate.Core\NanoMigrate.Core.csproj" />
    <!-- Referenced so the report smoke test can drive the shared migrate command. -->
    <ProjectReference Include="..\..\src\NanoMigrate.Cli.Commands\NanoMigrate.Cli.Commands.csproj" />
  </ItemGroup>

  <!-- The engine is organized into vertical-slice sub-namespaces; surface them all
       as global usings so the tests can reference Core types unqualified. -->
  <ItemGroup>
    <Using Include="nanoFramework.Migrate.Core.Projects" />
    <Using Include="nanoFramework.Migrate.Core.Solutions" />
    <Using Include="nanoFramework.Migrate.Core.Backup" />
    <Using Include="nanoFramework.Migrate.Core.Verification" />
    <Using Include="nanoFramework.Migrate.Core.Fleet" />
    <Using Include="nanoFramework.Migrate.Core.Common" />
    <Using Include="nanoFramework.Migrate.Core.Reporting" />
  </ItemGroup>

</Project>
